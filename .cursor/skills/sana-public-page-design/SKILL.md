---
name: sana-public-page-design
description: >-
  DEPRECATED arch reference for old Sana purple/gold public mirrors (sana-* classes).
  Do not use for new public-page work. Use tadris-lab-public-pages (Lasles + TADRIS LAB CX)
  instead. Only load when explicitly inspecting legacy sana-* HTML/CSS before migration.
---

# Sana Public Page Design — DEPRECATED

**Superseded by** `.cursor/skills/tadris-lab-public-pages/SKILL.md`.

- New public pages → Lasles (`lasles-*`) + teacher PD CX  
- Legacy files (`sana-*` / `geo-*` under `designs/public-pages/` and old Blade) = **arch** until migrated  
- Do not extend purple/gold Sana as the target system  

Inspect old CSS only to migrate a page away from it.
